version: "3"

services:
  estimate:
    container_name: estimate
    build: .
    ports:
      - 5000:5000
    networks:
      - charginginfrastructure_asset_network

networks:
  charginginfrastructure_asset_network:
    external: true


